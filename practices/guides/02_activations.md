# 02 激活函数

目标：理解并实现 Sigmoid、ReLU、Tanh、GELU 和 Softmax 的前向计算。

- 草稿：[`practices/activations.py`](../activations.py)
- 依赖：通过 `from .tensor import Tensor` 复用第 01 章张量
- 迁入：`src/jmtorch/nn/`

项目根目录运行 `python -m practices.activations`。其他章节可以使用 `from practices.activations import ReLU` 导入已有激活函数。
