# 01 张量

目标：存储、形状、切片、广播、基础运算。

- 草稿：[`practices/tensor.py`](../tensor.py)
- 迁入：`src/jmtorch/tensor/`
- 测试：`tests/test_tensor.py`

后续章节通过 `from practices.tensor import Tensor` 复用同一个张量类。先完成前向运算，再在第 06 章接入自动求导。
